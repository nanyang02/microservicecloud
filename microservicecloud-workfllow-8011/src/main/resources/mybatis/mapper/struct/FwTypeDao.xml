<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.atguigu.springcloud.dao.FwTypeDao">

    <!--<select id="findById" resultType="com.atguigu.springcloud.entities.Dept" parameterType="Long">-->
		<!--select-->
		<!--deptno, dname, db_source from dept where deptno=#{deptno};-->
	<!--</select>-->

    <select id="findAll" resultType="List">
		select deptno, dname, db_source from dept;
	</select>

    <insert id="addFwType" parameterType="WfType">
		INSERT INTO `wf_str_type`(`fw_name`, `desc`, `valid`) VALUES (#{fwName}, #{desc}, #{valid});
	</insert>

    <update id="updateName" parameterType="WfType">
        UPDATE dept SET dname = #{dname} WHERE deptno = #{deptno}
    </update>

</mapper>
